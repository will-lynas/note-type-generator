name: Rust CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - '**'
  workflow_dispatch:

jobs:
  rust-check:
    name: Run Various Cargo Checks
    runs-on: ubuntu-latest

    steps:
    - name: Run Checks
      uses: will-lynas/rust-check-ci@main
